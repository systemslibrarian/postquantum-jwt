<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>PostQuantum.Jwt.AspNetCore</RootNamespace>
    <AssemblyName>PostQuantum.Jwt.AspNetCore</AssemblyName>
    <IsTrimmable>true</IsTrimmable>
    <IsAotCompatible>true</IsAotCompatible>
    <!-- FROZEN at 1.0.0: superseded by PostQuantum.AspNetCore (its own repo,
         https://github.com/systemslibrarian/postquantum-aspnetcore). The
         nuget.org package is deprecated + unlisted; no further version is ever
         pushed. IsPackable=false makes `dotnet pack` produce nothing even if a
         pipeline globs this project — do not flip it back without deciding to
         un-supersede the package. Source stays buildable so in-repo samples,
         tests, and consumers on 1.0.0 keep working. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- NuGet package metadata -->
  <PropertyGroup>
    <PackageId>PostQuantum.Jwt.AspNetCore</PackageId>
    <Version>1.0.0</Version>
    <Title>PostQuantum.Jwt.AspNetCore — post-quantum JWT bearer auth for ASP.NET Core</Title>
    <Authors>Paul Clark</Authors>
    <Description>[SUPERSEDED — use PostQuantum.AspNetCore instead.] ASP.NET Core authentication handler for PostQuantum.Jwt — adds AddPqJwtBearer() so post-quantum (ML-DSA-65) tokens slot into the standard authentication pipeline. Includes an HTTP-fetched key ring (a JWKS-equivalent) for kid-based rotation across services. Fail-closed by construction. This package has been replaced by PostQuantum.AspNetCore (cleaner naming, dedicated release cadence, event-hook surface, hosted-service warmup, SignalR support). The new package is wire-compatible — tokens minted under either validate in the other. Migration guide: https://github.com/systemslibrarian/postquantum-aspnetcore/blob/main/docs/MIGRATION.md. Production-quality companion package for controlled issuer/verifier systems; NOT independently audited (a permanent, documented limitation — see KNOWN-GAPS.md) and not a drop-in OAuth/OIDC replacement.</Description>
    <PackageProjectUrl>https://github.com/systemslibrarian/postquantum-jwt</PackageProjectUrl>
    <RepositoryUrl>https://github.com/systemslibrarian/postquantum-jwt</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageTags>jwt;jose;jwe;post-quantum;pqc;ml-dsa;ml-kem;aspnetcore;authentication;jwtbearer;net10</PackageTags>
    <PackageReleaseNotes>SUPERSEDED. PostQuantum.Jwt.AspNetCore has been replaced by PostQuantum.AspNetCore — cleaner naming, dedicated release cadence, event-hook surface (OnMessageReceived / OnTokenValidated / OnAuthenticationFailed / OnChallenge), hosted-service key-ring warmup, full SignalR support, and a 35-test integration suite. Tokens minted by either package validate in the other (same engine). Migrate via the diff-style guide: https://github.com/systemslibrarian/postquantum-aspnetcore/blob/main/docs/MIGRATION.md. This package will continue to receive critical fixes through 1.0 but no new features. New consumers should adopt PostQuantum.AspNetCore.</PackageReleaseNotes>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <Copyright>Copyright © Paul Clark</Copyright>
    <NeutralLanguage>en-US</NeutralLanguage>
  </PropertyGroup>

  <PropertyGroup>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <EnablePackageValidation>true</EnablePackageValidation>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../PostQuantum.Jwt/PostQuantum.Jwt.csproj" />
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.300" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup>
    <None Include="../../README.md" Pack="true" PackagePath="\" />
    <None Include="../../LICENSE" Pack="true" PackagePath="\" />
    <None Include="../../CHANGELOG.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
