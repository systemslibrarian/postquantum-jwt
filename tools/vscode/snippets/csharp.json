{
  "PostQuantum.Jwt: Sign and validate": {
    "prefix": "pqjwt-sign",
    "description": "Sign a token with ML-DSA-65 and validate it (PostQuantum.Jwt).",
    "body": [
      "using System.Security.Cryptography;",
      "using PostQuantum.Jwt;",
      "",
      "using var signingKey = MLDsa.GenerateKey(MLDsaAlgorithm.MLDsa65);",
      "",
      "string token = new PqJwtBuilder()",
      "    .WithIssuer(\"${1:https://issuer.example}\")",
      "    .WithSubject(\"${2:user-123}\")",
      "    .WithAudience(\"${3:https://api.example}\")",
      "    .WithLifetime(TimeSpan.FromMinutes(${4:30}))",
      "    .WithClaim(\"${5:role}\", \"${6:admin}\")",
      "    .SignWith(signingKey)",
      "    .Build();",
      "",
      "using var verificationKey = MLDsa.ImportMLDsaPublicKey(",
      "    MLDsaAlgorithm.MLDsa65, signingKey.ExportMLDsaPublicKey());",
      "",
      "var validator = new PqJwtValidator(new PqJwtValidationParameters",
      "{",
      "    SignatureVerificationKey = verificationKey,",
      "    ValidIssuer   = \"${1:https://issuer.example}\",",
      "    ValidAudience = \"${3:https://api.example}\",",
      "});",
      "",
      "PqJwtValidationResult result = validator.Validate(token);",
      "$0"
    ]
  },
  "PostQuantum.Jwt: 60-second tour": {
    "prefix": "pqjwt-quick",
    "description": "Minimal sign + validate (the 60-second tour).",
    "body": [
      "using System.Security.Cryptography;",
      "using PostQuantum.Jwt;",
      "",
      "using var signingKey      = MLDsa.GenerateKey(MLDsaAlgorithm.MLDsa65);",
      "using var verificationKey = MLDsa.ImportMLDsaPublicKey(",
      "    MLDsaAlgorithm.MLDsa65, signingKey.ExportMLDsaPublicKey());",
      "",
      "string token = new PqJwtBuilder()",
      "    .WithSubject(\"${1:user-123}\")",
      "    .WithLifetime(TimeSpan.FromMinutes(${2:30}))",
      "    .SignWith(signingKey)",
      "    .Build();",
      "",
      "var result = new PqJwtValidator(new PqJwtValidationParameters",
      "{",
      "    SignatureVerificationKey = verificationKey,",
      "}).Validate(token);",
      "$0"
    ]
  },
  "PostQuantum.Jwt: Sign and encrypt": {
    "prefix": "pqjwt-encrypt",
    "description": "Sign-then-encrypt a token for an X-Wing recipient (PostQuantum.Jwt).",
    "body": [
      "using PostQuantum.Jwt;",
      "using PostQuantum.Jwt.Cryptography;",
      "",
      "// Recipient generates a key pair and publishes the public half.",
      "using var recipient = XWingPrivateKey.Generate();",
      "byte[] recipientPublic = recipient.PublicKey.Export();   // share this",
      "",
      "string token = new PqJwtBuilder()",
      "    .WithSubject(\"${1:confidential-subject}\")",
      "    .WithLifetime(TimeSpan.FromMinutes(${2:5}))",
      "    .SignWith(${3:signingKey})",
      "    .EncryptFor(XWingPublicKey.Import(recipientPublic))",
      "    .Build();",
      "",
      "var validator = new PqJwtValidator(new PqJwtValidationParameters",
      "{",
      "    SignatureVerificationKey = ${4:verificationKey},",
      "    DecryptionKey            = recipient,   // required for encrypted tokens",
      "});",
      "",
      "PqJwtValidationResult result = validator.Validate(token);",
      "$0"
    ]
  },
  "PostQuantum.Jwt: Key rotation and replay protection": {
    "prefix": "pqjwt-rotate",
    "description": "kid-based key rotation + jti replay protection (PostQuantum.Jwt).",
    "body": [
      "using PostQuantum.Jwt;",
      "",
      "string token = new PqJwtBuilder()",
      "    .WithKeyId(\"${1:signing-key-2026}\")",
      "    .WithJwtId(Guid.NewGuid().ToString(\"N\"))",
      "    .WithLifetime(TimeSpan.FromMinutes(${2:5}))",
      "    .SignWith(${3:signingKey})",
      "    .Build();",
      "",
      "var validator = new PqJwtValidator(new PqJwtValidationParameters",
      "{",
      "    // Pick a verification key from the token's kid (key rotation).",
      "    SignatureKeyResolver = kid => ${4:keyRing}.TryGetValue(kid, out var k) ? k : null,",
      "    // Reject any jti seen before. InMemoryReplayCache is single-process;",
      "    // implement IPqJwtReplayCache over a shared store for multi-node setups.",
      "    ReplayCache = new InMemoryReplayCache(),",
      "    RequireReplayProtection = true,",
      "});",
      "$0"
    ]
  },
  "PostQuantum.Jwt: ASP.NET Core (PostQuantum.AspNetCore)": {
    "prefix": "pqjwt-aspnetcore",
    "description": "Wire post-quantum bearer auth into ASP.NET Core (AddPostQuantumJwtBearer).",
    "body": [
      "using System.Security.Cryptography;",
      "using PostQuantum.Jwt;",
      "using PostQuantum.AspNetCore;",
      "",
      "var builder = WebApplication.CreateBuilder(args);",
      "",
      "builder.Services",
      "    .AddAuthentication(PqJwtBearerDefaults.AuthenticationScheme)",
      "    .AddPostQuantumJwtBearer(options =>",
      "    {",
      "        var keyBytes = Convert.FromBase64String(",
      "            builder.Configuration[\"Auth:VerificationKey\"]",
      "                ?? throw new InvalidOperationException(\"Missing Auth:VerificationKey\"));",
      "        options.ValidationParameters = new PqJwtValidationParameters",
      "        {",
      "            SignatureVerificationKey = MLDsa.ImportMLDsaPublicKey(",
      "                MLDsaAlgorithm.MLDsa65, keyBytes),",
      "            ValidIssuer   = builder.Configuration[\"Auth:Issuer\"],",
      "            ValidAudience = builder.Configuration[\"Auth:Audience\"],",
      "            ReplayCache   = new InMemoryReplayCache(),",
      "        };",
      "    });",
      "builder.Services.AddAuthorization();",
      "",
      "var app = builder.Build();",
      "app.UseAuthentication();",
      "app.UseAuthorization();",
      "",
      "app.MapGet(\"/me\", (HttpContext ctx) => new",
      "{",
      "    sub  = ctx.User.FindFirst(\"sub\")?.Value,",
      "    role = ctx.User.FindFirst(\"role\")?.Value,",
      "}).RequireAuthorization();",
      "",
      "app.Run();",
      "$0"
    ]
  },
  "PostQuantum.Jwt: HTTP key ring (JWKS-equivalent)": {
    "prefix": "pqjwt-keyring",
    "description": "Resolve verification keys from a trusted HTTPS endpoint (HttpPqJwtKeyRing).",
    "body": [
      "builder.Services.AddHttpClient<HttpPqJwtKeyRing>();",
      "builder.Services.AddSingleton(sp =>",
      "{",
      "    var http = sp.GetRequiredService<IHttpClientFactory>().CreateClient(nameof(HttpPqJwtKeyRing));",
      "    return new HttpPqJwtKeyRing(http, new Uri(builder.Configuration[\"Auth:KeysEndpoint\"]!));",
      "});",
      "$0"
    ]
  },
  "PostQuantum.Jwt: Install (PackageReference)": {
    "prefix": "pqjwt-install",
    "description": "PackageReference for PostQuantum.Jwt.",
    "body": [
      "<PackageReference Include=\"PostQuantum.Jwt\" Version=\"${1:1.0.0-preview.5}\" />$0"
    ]
  }
}
