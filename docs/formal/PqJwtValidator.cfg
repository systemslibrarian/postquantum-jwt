\* TLC model-check configuration for PqJwtValidator.tla.
\* Run:  tlc PqJwtValidator.tla   (TLC reads this .cfg by convention)
\* The state space is tiny (768 token-fact combinations); it checks in well
\* under a second.

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoAcceptWithoutVerify
INVARIANT AcceptIsSound

PROPERTY Termination
